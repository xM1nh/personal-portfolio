import './_Header.css'

const Header = () => {
    return (
        <header>
            <nav className="navBar">
                <a>Projects</a>
                <a>Contact</a>
            </nav>
        </header>
    )
}

export default Header